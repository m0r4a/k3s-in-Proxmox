# Example of the fragment written by the storage root (terraform/storage).
# The real file is inventory.d/20-storage.ini (generated, gitignored).
# Copy this to 20-storage.ini to run Ansible without Terraform.

[storage]
10.0.0.20 ansible_user=mora

[storage:vars]
ansible_ssh_private_key_file=~/.ssh/id_ed25519
cluster_admin_user=mora
cluster_admin_home=/home/mora
